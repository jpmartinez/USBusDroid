<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tecnoinf.proyecto.grupo4.usbusdroid3">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainClient" />
        <activity android:name=".Activities.NewTicket.NewTicket" />
        <activity android:name=".Activities.MyTickets.MyTickets" />
        <activity android:name=".Activities.TimeTable.TimeTable" />
        <activity android:name=".Activities.ContactUs.ContactUs" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="37639548c5fc33add920f563bac6215f2740df48" />

        <activity android:name=".Activities.NewTicket.NTJourneyListActivity" />
        <activity android:name=".Activities.NewTicket.NTSelectSeatActivity" />
        <activity android:name=".Activities.NewTicket.NTConfirmationActivity" />
        <activity android:name=".Activities.NewTicket.NTResultActivity" />
        <activity android:name=".Activities.TimeTable.TTServicesListActivity"></activity>
    </application>

</manifest>